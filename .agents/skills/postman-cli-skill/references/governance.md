# Postman CLI API Governance

Postman CLI can lint OpenAPI schemas locally.

## Linting a Local File
`postman api lint <path_to_openapi.yml>`

## Using Cloud Rulesets
By default, this uses Postman's built-in rules. If logged in, you can apply workspace-specific rulesets configured by your team.
